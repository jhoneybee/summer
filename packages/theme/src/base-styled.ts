import { css } from 'styled-components'

export const disable = css`
    cursor: default;
    pointer-events: none;
    background: #f5f5f5;
    border-color: #d9d9d9;
    box-shadow: none;
    color: rgba(0,0,0,.25);
    text-shadow: none;
`